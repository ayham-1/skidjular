name: Windows C/C++ CI

on: [push]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: |
        cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug .
        make -j9
