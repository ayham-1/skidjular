name: Windows C/C++ CI

on: [push]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: |
        mkdir build
        cmake -DCMAKE_BUILD_TYPE=Debug build/
        make -j9 -f build/Makefile
